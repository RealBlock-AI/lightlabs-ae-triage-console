import { supportPerformance } from "@/lib/supportDemo";
import { Activity, Bot, CheckCircle2, Clock3, UserRoundCheck } from "lucide-react";

export default function SupportPerformance() {
  const points = supportPerformance.trend.map((point, index) => `${(index / (supportPerformance.trend.length - 1)) * 560 + 20},${190 - (point.automated / 100) * 150}`).join(" ");
  return <section className="mx-auto max-w-7xl p-5 md:p-10"><header className="grid gap-6 border-b border-slate-200 pb-8 lg:grid-cols-[1.25fr_.75fr]"><div><p className="eyebrow">SUPPORT PERFORMANCE</p><h1 className="mt-2 font-serif text-4xl font-semibold tracking-tight text-slate-950 md:text-5xl">Measure service quality, then improve the handoff.</h1><p className="mt-4 max-w-3xl text-base leading-7 text-slate-600">This replaces theoretical capacity math with support operations metrics: how quickly verified requests receive a response, how quickly they reach resolution, and whether the AI agent can answer safely without human intervention.</p></div><aside className="rounded-2xl border border-blue-200 bg-blue-50 p-5"><Activity className="text-blue-700" size={22}/><h2 className="mt-4 text-sm font-bold text-blue-950">Why it matters</h2><p className="mt-2 text-sm leading-6 text-blue-900">Automation should earn trust through better customer outcomes. Feedback from reviewed cases is measured over time before an automation rule changes.</p></aside></header><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={<Clock3 size={19}/>} label="Avg. first response" value={`${supportPerformance.firstResponseMinutes} min`} detail="Verified Slack inquiries"/><Metric icon={<CheckCircle2 size={19}/>} label="Avg. resolution" value={`${supportPerformance.resolutionMinutes} min`} detail="Across resolved support cases"/><Metric icon={<Bot size={19}/>} label="AI resolved" value={`${supportPerformance.automatedRate}%`} detail="No human review required" tone="emerald"/><Metric icon={<UserRoundCheck size={19}/>} label="Human review" value={`${supportPerformance.humanReviewRate}%`} detail="AE or AM oversight retained" tone="amber"/></div><div className="mt-7 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><section className="panel"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">FEEDBACK-LOOP PROGRESS</p><h2 className="mt-1">AI resolution rate is improving</h2></div><span className="text-sm font-bold text-emerald-700">+27 pts since March</span></div><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Each point reflects the share of support questions that closed through an approved AI workflow. Escalations and assisted cases stay with people; their review outcomes provide the next set of improvements.</p><div className="mt-6 rounded-xl border border-slate-200 bg-slate-50 p-4"><svg className="h-56 w-full" viewBox="0 0 600 230" preserveAspectRatio="none" aria-label="Monthly AI resolution rate trend"><defs><linearGradient id="support-fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#86efac" stopOpacity=".45"/><stop offset="100%" stopColor="#86efac" stopOpacity="0"/></linearGradient></defs><line x1="20" y1="190" x2="580" y2="190" stroke="#cbd5e1"/><polyline fill="none" stroke="#15803d" strokeWidth="5" points={points}/>{supportPerformance.trend.map((point, index) => <g key={point.label}><circle cx={(index / (supportPerformance.trend.length - 1)) * 560 + 20} cy={190 - (point.automated / 100) * 150} r="6" fill="#15803d"/><text x={(index / (supportPerformance.trend.length - 1)) * 560 + 20} y="218" textAnchor="middle" fill="#64748b" fontSize="12">{point.label}</text><text x={(index / (supportPerformance.trend.length - 1)) * 560 + 20} y={176 - (point.automated / 100) * 150} textAnchor="middle" fill="#166534" fontSize="12" fontWeight="700">{point.automated}%</text></g>)}</svg></div></section><aside className="panel"><p className="eyebrow">30-DAY DEMO WINDOW</p><h2 className="mt-1">How work was handled</h2><div className="mt-5 space-y-4"><Breakdown label="Fully automated" value={78} color="bg-emerald-500" detail="Customer received an approved answer"/><Breakdown label="Human reviewed" value={48} color="bg-amber-500" detail="AE/AM confirmed the response"/></div><div className="mt-6 rounded-xl bg-slate-950 p-4 text-sm leading-6 text-slate-200"><b className="text-emerald-300">Learning loop:</b> review reason → revise knowledge or policy → observe the next cohort → approve any automation expansion.</div></aside></div><p className="mt-5 text-xs leading-5 text-slate-500">The performance values are demonstration telemetry for the prototype. In production, they would be computed from persisted Slack and email support events, response timestamps, resolution states, and review outcomes.</p></section>;
}

function Metric({ icon, label, value, detail, tone = "slate" }: { icon: React.ReactNode; label: string; value: string; detail: string; tone?: "slate" | "emerald" | "amber" }) { const tones = { slate: "bg-slate-100 text-slate-700", emerald: "bg-emerald-50 text-emerald-700", amber: "bg-amber-50 text-amber-700" }; return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><span className={`grid h-9 w-9 place-items-center rounded-xl ${tones[tone]}`}>{icon}</span><p className="mt-5 text-xs font-bold uppercase tracking-[.12em] text-slate-500">{label}</p><p className="mt-1 font-serif text-3xl font-semibold text-slate-950">{value}</p><p className="mt-1 text-sm text-slate-500">{detail}</p></article>; }
function Breakdown({ label, value, color, detail }: { label: string; value: number; color: string; detail: string }) { const percent = Math.round((value / supportPerformance.totalInquiries) * 100); return <div><div className="flex items-baseline justify-between gap-3"><div><p className="font-semibold text-slate-900">{label}</p><p className="text-xs text-slate-500">{detail}</p></div><b className="font-serif text-2xl text-slate-950">{value}</b></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${color}`} style={{ width: `${percent}%` }}/></div><p className="mt-1 text-right text-xs text-slate-500">{percent}% of {supportPerformance.totalInquiries} inquiries</p></div>; }
