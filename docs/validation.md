# Permanent App Validation Notes

The permanent preview rendered the AE queue without the former fallback-page routing defect. The visible queue includes ownership-filtered account rows, text-and-color lane badges, tier, category, SLA, and confidence marked as non-dispositive. The Appendix A controls, persona selector, and capacity navigation are present.

The Appendix A out-of-spec fixture rendered a decision packet containing an ordered lane-reason chain, a confidence value explicitly described as non-dispositive, source-attributed evidence, an inline `NOT CITABLE` marker, attributed precedent, posture summary, and a disabled send control. The capacity view rendered lane mix, an assumption-based status badge, a labelled assumption-based handling model, and its learning loop.

Remaining validation covers cross-AE access denial and the HTTP ingestion boundary.

The permanent route was requested as Marcus for a Sarah-owned interaction and returned the access-denied state without exposing interaction evidence. HTTP validation successfully completed the Slack URL-verification challenge and a first-plus-retry ingestion sequence returned success twice while retaining a single idempotent interaction record.

Mobile full-page verification confirmed the queue retains its text-and-color lane badges, non-dispositive confidence cue, demo controls, and visible safety state, while capacity cards collapse into a readable single-column flow.
